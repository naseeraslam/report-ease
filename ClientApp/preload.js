// All of the Node.js APIs are available in the preload process.
// It has the same sandbox as a Chrome extension.
// We can expose specific APIs to the renderer process (our React app)
// in a controlled way here if needed. For now, we don't need any.